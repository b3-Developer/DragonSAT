# DragonSAT Docker deployment environment
# Copy this file to .env and fill in your values.
# docker-compose reads .env automatically from this directory.

# ── Backend ───────────────────────────────────────────────────────────────────
NODE_ENV=production
PORT=3001
DATABASE_PATH=/app/data/dragonsat.db
OPENSAT_API_URL=https://pinesat.duckdns.org/api/questions
FRONTEND_URL=http://localhost:3000

# Generate a strong random secret: openssl rand -hex 32
JWT_SECRET=change-me-to-a-long-random-secret

# ── Google OAuth (optional) ───────────────────────────────────────────────────
# Set both to enable Google Sign-In. Leave blank to disable.
# Get credentials at https://console.cloud.google.com/
# Note: NEXT_PUBLIC_GOOGLE_CLIENT_ID is baked into the published image at build time — not set here.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
